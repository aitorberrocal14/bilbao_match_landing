node_modules/
.DS_Store

# Built by `cd wordpress && zip -r match-bilbao-bizkaia.zip match-bilbao-bizkaia`
# for uploading to WordPress; regenerate it rather than committing it.
wordpress/match-bilbao-bizkaia.zip

# The platform key lives here when syncing by hand; never committed.
.env

# The administration panel's password. Lives on the server only.
admin/admin-config.php

# The upload package. Regenerate with `node tools/build-upload.js` rather than
# committing 5 MB that changes with every edit to the site.
dist/web-para-subir.zip
dist/_subir/
