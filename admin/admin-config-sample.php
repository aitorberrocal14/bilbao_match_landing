<?php
/**
 * PASSWORD FOR THE ADMINISTRATION PANEL
 * -----------------------------------------------------------------------------
 * Copy this file to `admin-config.php` on the server and change the password.
 * While `admin-config.php` does not exist, the panel cannot write anything and
 * falls back to downloading the files.
 *
 * `admin-config.php` is never committed and never uploaded from the repository:
 * it lives only on the server, so the password is not in the project's history.
 *
 * Use a long password. It is the only thing between the internet and the
 * contents of the site.
 */

return 'CAMBIA-ESTA-CONTRASENA';
